{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Porter Build",
            "type": "shell",
            "command": "porter build -v",
            "dependsOn": [
            ],
            "options": {
                "cwd": "${workspaceFolder}/automation/openshift/ocp3.11/cnab"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}